import React from "react";
import AppLayout from "../layouts/AppLayout/AppLayout";
import "./errorPages.scss";

const ErrorPages = ({ children }) => {
  return <AppLayout>{children}</AppLayout>;
};

export default ErrorPages;
